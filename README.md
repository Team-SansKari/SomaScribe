start with assessment 1

if goto_next_assessment is True add 1 to the assessment value. In this case next_question will come null.

if unlikely_depression is TRUE then no depression and we will handle other case later. In this case next_question will come null.

if next_question is "PROTOCOL 1" OR "BIPOLAR PROTOCOL" then use the next endpoint to obtain protocol content.




add .env file with API_KEY = "your_api_key"